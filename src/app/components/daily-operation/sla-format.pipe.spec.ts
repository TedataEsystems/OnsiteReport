import { SlaFormatPipe } from './sla-format.pipe';

describe('SlaFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SlaFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
